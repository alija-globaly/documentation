# Orderlay ArgoCD GitOps Rollout — Status Summary

*Copied from the auto-memory record `orderlay_argocd_gitops_rollout.md` on 2026-09-16, for quick human reading alongside the full session log. The memory itself remains the live, auto-recalled source of truth — this file is a snapshot and will not be kept in sync automatically.*

---

orderlay's K8s cluster already runs ArgoCD syncing the shared `argocd-gitops` repo (cluster addons: ingress-nginx, cert-manager) via App-of-Apps — this part works and is shared with agentcis's cluster. What orderlay lacks is an app-workload GitOps repo analogous to `gitops-agentcisapp-deployments` (which drives agentcis-app's actual containers). Full mental model and file-by-file trace in `mainapp/ORDERLAY_ARGOCD_GITOPS_SESSION_NOTES_2026-09-10.md`.

**Key architectural conclusion from that note:** orderlay uses plain `ingress-nginx` (hostPort 80/443, no AWS ALB/Gateway API), unlike agentcis. So orderlay's version of the app-deployment repo should NOT need `gitops-agentcisapp-deployments`'s `pre-apps`/`public-traffic` AWS ALB/Gateway-API machinery — just cluster addons (done) → one service's `Application` CR + values file. The generic chart `GH-infra-and-k8s-charts-central/helm-charts/self-managed/backend` already supports `ingress.className: nginx` and NFS-mounted `.env` (`volumes.defaultpvc`), matching orderlay's existing env-file pattern — strong reuse candidate, no new chart needed.

**Correction (2026-09-11, direct from user):** the note's ingress-nginx conclusion is superseded — user has deliberately decided to follow agentcis's AWS Gateway API / ALB pattern instead, not plain ingress-nginx, specifically because upstream `kubernetes/ingress-nginx` has reached end-of-life. The `gitops-orderlay-deployments` scaffold's AWS Gateway API pieces are intentional, not a stale leftover. Full detailed session log of the build-out is now written into `ORDERLAY_ARGOCD_GITOPS_SESSION_NOTES_2026-09-10.md` §11 (added 2026-09-11) — read that section for exact file states/line numbers rather than relying on this summary for specifics.

**Key corrected/resolved findings from the 2026-09-11 build session (see note §11 for full detail):**
- **AppProject**: confirmed LIVE (via `kubectl get appprojects -n argocd` on both real agentcis clusters) that agentcis runs entirely on the built-in `project: default` — no scoped AppProject exists anywhere in production or staging. Orderlay's scaffolded dedicated `projects/orderlay.yml` was deleted to match; all root Applications use `project: default`.
- **Branch**: `bootstrap/staging-root.yml` now uses `targetRevision: "live-values"` (matching agentcis), though that branch doesn't exist yet in `gitops-orderlay-deployments` — accepted forward-reference until the file structure is finished.
- **IRSA-role finding (important, corrects an earlier overcautious read of the Terraform/IAM/ASG bootstrap note)**: the role named `<project>-<env>-K8s-EC2-IRSA-role` is NOT real IRSA/OIDC — its trust policy is `ec2.amazonaws.com`, a plain EC2 instance-profile role, unrelated to the `oidc_create` flag (which only gates a separate GitHub-Actions OIDC resource). This role + its instance profile + the full AWS Load Balancer Controller IAM policy are already auto-attached to EVERY node (master/workers/ASG) via the shared `main-template.tf`, for both agentcis and orderlay, the moment `terraform apply` runs. No new AWS/IAM provisioning was needed. Confirmed live in the AWS console: `orderlay-staging-K8s-EC2-IRSA-role` attached to `[orderlay-staging]-k8s-root-master-nginx`, VPC `vpc-0956141e90f4f7a4e`, account `381491939487`, `ap-south-1`.
- **`nodeAffinity` pinning to a `role=aws-elb-master` node in agentcis's reference values file is confirmed dead code** — verified live on agentcis-staging that no node carries that label (`kubectl get nodes -l role=aws-elb-master` → no resources). Dropped from orderlay's `gateway-api-aws.yml` entirely rather than copied forward.
- **Live, unresolved bug found**: orderlay's ArgoCD repo-credential Secret for `gitops-orderlay-deployments` (`gitops-ansible-github-secret-github-secret` in the `argocd` namespace) actually points at `GH-infra-and-k8s-charts-central.git` (the infra repo) with agentcis's token identity (`agentcisapp-argocd-token`) — because the shared, non-project-scoped `ansible-config-mgmt/.env` had `GITOPS_REPO_URL` mistakenly set equal to `ARGO_REPO_URL` when the credential-setup Ansible task was run from another laptop on 2026-09-10. **As of 2026-09-11, orderlay's ArgoCD has zero working credential for its own app-workload repo** — fix requires a new GitHub PAT scoped to `gitops-orderlay-deployments`, then either a direct `kubectl apply` replacing the Secret and/or fixing `.env` + re-running `make orderlay-staging-setup-argocd` (doing only the former without the latter means the next Ansible re-run silently reverts the fix, since `.env` is the actual source of truth).
- A **separate, parallel track** is already live-cutting `backend_v2` to nginx ingress directly on the server: `orderlay/.github/workflows/nginx-k8s-backend_v2.yml` (added 2026-09-11) still uses the old SSH+`sed`+`make up-new-backend_v2` pipeline against `hpa-backend_v2-nginx.yml` — not yet wired to `gitops-orderlay-deployments` at all, and now looks like a stopgap/different track from the ALB-based GitOps plan rather than its pilot. Not yet clarified with the user whether this nginx track is being abandoned in favor of ALB, or is unrelated.

**Open decisions (per note §9, still unresolved):**
- Pilot service not chosen: `backend_v2` (core API, proposed) vs `web-v2` (simpler) vs `notification-service` (lowest blast radius).
- Whether to create the GitHub repo first or draft locally against a placeholder URL.
- How ArgoCD gets repo credentials for `gitops-orderlay-deployments` (extend `1.2-repo-secret-setup.yml` ansible task vs `argocd repo add` directly).
- Whether the generic `backend` chart fits every service as-is or needs tweaks (e.g. `order-service`'s gRPC port 8089, `web-v2`'s Next.js build).
- Scope is staging-only for now; production is a later phase.

**2026-09-16 session (note §16): `backend_v2` GitHub Actions OIDC fixed end-to-end.** Root cause of the pipeline's `AssumeRoleWithWebIdentity` failures was `oidc_create = false` in orderlay staging tfvars — no AWS IAM OIDC identity provider for `token.actions.githubusercontent.com` existed in orderlay's account (`381491939487`), even though the IAM role that needs it (`orderlay-staging-github-actions-AWS-ECR-S3-IRSA-role`) is created unconditionally by Terraform. Fixed by creating the OIDC provider **manually** via AWS console (not `terraform apply`, to avoid touching state/servers) — confirmed safe first by tracing `main-template.tf` and finding that module fully standalone. After that, a second error (`Not authorized to perform sts:AssumeRoleWithWebIdentity`) turned out to be unrelated to the OIDC provider or the role's trust policy (both were fine) — the GitHub Actions repo variable `AWS_GITHUB_ROLE_ARN` (Staging environment) was simply pointing at the wrong role (`orderlay-staging-K8s-EC2-IRSA-role`, the unrelated EC2 instance role) due to a hand-typed ARN, since the user's IAM console permissions didn't allow browsing/listing Roles to copy it directly. Fixed the variable; pipeline now works end-to-end through ECR login. Follow-up: added `iam:ListRoles`/`iam:ListPolicies`/`iam:UpdateAssumeRolePolicy`/etc. to a **local copy** of the shared IAM policy at `~/Downloads/TerraformInfrastructurePolicy.json` — **not yet pushed to AWS**, and not yet reconciled against the canonical live file `/home/alija/TerraformInfrastructurePolicy.json` (a third, distinct path from both this Downloads copy and the already-known-abandoned `~/Downloads/orderlay-terraform-deployer-policy.json`) — diff these before pushing anything.

See also `ORDERLAY_TERRAFORM_BOOTSTRAP_IAM_AND_ASG_LIFECYCLE_NOTES.md` for how the underlying cluster/server got created.
